<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book.errors.count, t("book.error")) %> <%= t("book.error-message") %></h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label t("book.title"), style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label t("book.memo"), style: "display: block" %>
    <%= form.text_area :memo %>
  </div>

  <div>
    <%= form.label t("book.author"), style: "display: block" %>
    <%= form.text_field :author %>
  </div>

  <div>
    <%= form.label t("book.picture"), style: "display: block" %>
    <%= form.file_field :picture %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
