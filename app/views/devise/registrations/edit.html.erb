<h2><%= t(".page_title") %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div>
    <b><%= User.human_attribute_name(:name) %></b>：<br>
    <%= f.text_field :name %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div>
    <b><%= User.human_attribute_name(:email) %></b>：<br>
    <%= f.text_field :email %>
  </div>

  <div>
    <b><%= User.human_attribute_name(:post_code) %></b>：<br>
    <%= f.text_field :post_code %>
  </div>

  <div>
    <b><%= User.human_attribute_name(:address) %></b>：<br>
    <%= f.text_field :address %>
  </div>

  <div>
    <b><%= User.human_attribute_name(:introduction) %></b>：<br>
    <%= f.text_area :introduction %>
  </div>

  <div class="field">
    <b><%= User.human_attribute_name(:password) %></b>：
    <i>(<%= t(".leave_blank") %>)</i><br>
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> <%= t(".characters_minimum") %></em>
    <% end %>
  </div>

  <div class="field">
    <b><%= User.human_attribute_name(:password_confirmation) %></b>：<br>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <b><%= User.human_attribute_name(:current_password) %></b>：
    <i>(<%= t(".confirm_current_password") %>)</i><br>
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>

<p>
  <%= button_to t("button.Cancel_my_account"), registration_path(resource_name), data: { confirm: t("confirmations.cancel_account"), turbo_confirm: t("confirmations.cancel_account") }, method: :delete %>
</p>

<p>
  <%= link_to t("button.back"), :back %>
</p>
